name: flora
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^1.0.0
  charts_flutter: ^0.12.0
  image_picker: ^0.8.4+4

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true
